apiVersion: deployments.plural.sh/v1alpha1
kind: GlobalService
metadata:
  name: k8s-demo-single-namespace
  namespace: infra
spec:
  template:
    name: k8s-demo-single-namespace
    namespace: default #default — deploys the service into an existing namespace
    git:
      folder: charts/k8s-demo-single-namespace
      ref: main
    repositoryRef:
      kind: GitRepository
      name: infra
      namespace: infra
    syncConfig:
      deleteNamespace: false # don't delete default ns if the service is removed.